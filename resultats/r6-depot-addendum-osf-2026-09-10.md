# Preuve locale du dépôt de l’addendum R6

La registration [R6 sur OSF](https://osf.io/kmqnw/) est identifiée par la clé `kmqnw`. Son titre affiché est **popsim R6 représentation camp API v2 reprise 10 sept**. L’interface indique `Registered` le 10 septembre 2026 à 21 h 34 min 57 s CEST, le statut `Pending approval`, la licence **CC BY 4.0**, et une narrative intégrale visible.

Aucun fichier n’est sélectionné dans cette registration. Aucun DOI n’est attribué.

Le dépôt a été précédé du GO de la revue Euler. Le pilote R6 a ensuite été bloqué par le contrôle de prix avant tout appel, avec `0` POST et `0 USD` engagés.

Cette preuve repose sur l’observation de l’interface OSF fournie par le parent. Elle n’est donc pas une vérification indépendante de l’interface, de la registration ou de son archivage.

