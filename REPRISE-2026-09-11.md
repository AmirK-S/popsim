# Point d'entrée pilotage, 11 septembre 2026 (mise à jour)

Relevé courant, établi en lecture seule sur les fichiers locaux et les métadonnées du
registre : aucun appel réseau, aucune génération, aucune écriture. Historique bref : la
nuit du 10 au 11/09 a vu un arrêt sur 429 (`colhomo`, 00:49:49), la garde `STOP-R6`
réinstallée manuellement, puis un amendement 429 v1 activé et mis en échec en production
par un incident (`wlthhsps`, 13:56). Détail dans git log et les documents cités plus bas.

## État unique courant

- **R2b : terminé.** 180 personnes complètes.
- **R7 : Final actif.** Aucun relevé chiffré nouveau sans source autoritative.
- **R6 : pas de campagne active.** `data/traces/STOP-R6` présent, posé automatiquement par
  le runner à 14:05:09 (correctif : le runner pose désormais lui-même la garde, sans
  jamais écraser un STOP existant, dès qu'il refuse de continuer).
- **R6, avancement F1 :** `240 / 894` lignes dans la trace de campagne. F2 (316) et
  plancher (40) non commencés.
- **R6, les quatre 429 avec `generation_id` sont tous prouvés non facturés** : `fucitzn`
  ×2, `colhomo` (`resultats/r6-preuve-reconciliation-colhomo-2026-09-11.md`) et `wlthhsps`
  (`resultats/r6-preuve-reconciliation-wlthhsps-2026-09-11.md`, écart `9,7e-9 USD`), chacun
  par rapprochement manuel (ancre et variation des coûts réglés).
- **R6, registre :** aucune réservation ouverte (`r6-v2-registre-global.jsonl` : 387
  réservations − 363 réconciliations − 24 annulations = 0). DeepSeek réglé
  `0,0041715030 USD` ; total R6 réglé `0,0380760530 USD`. Plafond `4,40 USD` : le budget
  n'est pas le facteur limitant.
- **Amendement 429, v1 :** activé, mais son critère d'égalité stricte du solde a **échoué
  en production** — le solde met 15 à 40 s à se régler après un appel antérieur, ce qui
  rend l'égalité stricte intenable même sans facturation réelle.
- **Amendement 429, v2 :** écrite (`resultats/r6-amendement-429-v2-2026-09-11.md`), ancre
  stable par run et critère par variation des coûts réglés, preuve toujours persistée.
  Code testé (`test_r6_amendement_429.py`, 129 vérifications, exit 0). **Revue adverse en
  cours ; `AMENDEMENT_429_SHA256 = None` : v2 n'est pas activée.**
- **Twin A→B (tournoi diagnostic contre exactitude, Twin-2K-500) :** audit de provenance
  terminé, partition figée (SHA dans `resultats/tab-preenregistrement.md`). Préenregistrement
  en **version candidate**, non déposé. Code d'évaluation figé (`analyses/tab_evaluer.py`,
  24 tests synthétiques, garde `GO-TAB` absente). **Aucun résultat Twin calculé.**
- **Matrice de revendications :** terminée. Fidélité de groupe, correspondance à la
  personne et utilité prédictive sont trois cibles distinctes ; la troisième (objet du
  tournoi Twin A→B) n'est pas démontrée.
- **Publication :** environnement déclaré (`pyproject.toml`, `requirements.txt`,
  `reproductibilite/`). Licences vérifiées : OSF t6g7k et GSS restent en renvoi, non
  redistribuables. Revue de divulgation terminée : aucune ligne de personne ; deux fichiers
  **déjà suivis par Git** exposent un chemin local de l'opérateur —
  `resultats/mesures-a4/a4-20260903-145751.json` (télémétrie brute) et
  `resultats/a11_twin_resultats-smoke.json` (un seul champ) — à corriger avant publication.

## Décisions en attente (parent)

1. **Amendement 429 v2** : conclure la revue adverse, sceller le SHA-256 dans
   `analyses/r6_runner_campagnes.py`, écrire une preuve d'activation datée, avant toute
   reprise de campagne DeepSeek.
2. **Twin A→B** : valider le texte du préenregistrement, trancher les précisions 7.2 et les
   fragilités L15 (levier de régression) et L16 (~4 % des réplicats bootstrap sans ligne de
   matrice à K > 2), puis déposer sur OSF avant la première exécution réelle de
   `tab_evaluer.py` (avant de créer `data/traces/GO-TAB`).
3. **Corriger les deux fichiers suivis** exposant un chemin local avant publication.
4. **Rejouer une seule fois** les quatre cellules 429 non facturées, sous la version
   d'amendement finalement retenue.

## Garde-fous conservés

- Ne pas présenter le pilote DeepSeek comme le run R6 complet ; son critère de déterminisme
  a échoué (1/10, seuil 7/10), modèle descriptif, non déterministe.
- Ne pas relancer ni étendre des appels R6 payants sans GO explicite, bornes valides, garde
  `STOP-R6` levée sciemment et amendement 429 tranché (v1 en l'état, ou v2 scellée).
- Ne pas actualiser R7 à partir d'un relevé non autoritatif.
- Ne pas lire Twin-2K-500 réellement avant dépôt OSF du préenregistrement et création
  délibérée de `data/traces/GO-TAB`.
- Conserver les statuts OSF comme « enregistrés, Pending approval, sans DOI ».

## Recherche prête à avancer sans dépense

Le tournoi Twin A→B est entièrement local (13 configurations LLM, humains vague 4), sans
appel ni euro. Seul obstacle : validation du responsable puis dépôt OSF, avant la première
exécution du code déjà figé.

## Actions du responsable

- Valider le texte du préenregistrement Twin A→B, trancher 7.2 et L15/L16, déposer sur OSF.
- Statuer sur l'amendement 429 v2 (revue adverse) et, si retenu, sceller son SHA-256.
- Décider du sort des deux fichiers Git exposant un chemin local avant publication.
- Autoriser un commit de l'état courant du dépôt.

## État Git

`145` entrées non suivies ou modifiées. Rien n'a été commité depuis `e29a536` (licences).
Un commit de cet état reste le prochain geste documentaire utile.
